pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('admin-module') {
                    bat '"C:\\apache-maven-3.9.15\\bin\\mvn.cmd" clean compile'
                }
            }
        }

        stage('Test') {
            steps {
                dir('admin-module') {
                    bat '"C:\\apache-maven-3.9.15\\bin\\mvn.cmd" test'
                }
            }
        }
    }
}